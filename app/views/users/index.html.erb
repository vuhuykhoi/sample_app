<% provide :title, t("page.all_users") %>
<h1><%= t("page.all_users") %></h1>

<%= paginate @users, theme: "twitter-bootstrap-3" %>

<ul class="users">
  <% @users.each do |user| %>
   <%= render user %>
  <% end %>
</ul>

<%= paginate @users, theme: "twitter-bootstrap-3" %>
